Can copy files from local to image.
it can unzip the file 
and it can donwload the file directly into image

ADD includes all the functionality of COPY but adds two extra features:

It can copy files from remote URLs directly into the image.

It can automatically extract local tar archives (compressed files) into the destination location inside the image.
