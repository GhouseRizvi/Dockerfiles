Docker file is the declerative way of creating image.
Docker define its own sysntaxto create a image using the Dockerfiles.

## Building and image ##
docker built -t [docker hub url][/username][/imagename/]: version
