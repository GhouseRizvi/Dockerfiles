How to create a docker images
we can create a docker images uisng the docker file.
docker file is a declerative way of creating docker images

### how to build image from docker file ###
To build a image from docker file we need to run the below commands.

docker build -t [docker-hub-URL]/[your-username]/[imagename]:version

docker buld -t From:v1 . tags are optional except image name.


