It is used to exit the container
By default docker request for exit and wait for sometime if its not exiting we can force kill, by using the stop signal

When stop signal recived application can closed all the network and db connection and close automatically.
