Entry Point is initialized at the time of container Running.
It cannot be overriten 
CMD can be overidden at Runtime ENtryPoint Cannot, it restrict the user to modifyut the default values.
We cannot override the entrypoint, if we can itll append to default values of entrypoint.

For best result we can use the entrypoint with cmd
