COPY Instructions

Copy is used to copy the file from local to image
The COPY instruction in a Dockerfile is used to copy files and directories from the local build context (your host machine) into the Docker image at a specified destination path.