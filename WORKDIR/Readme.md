WORKDIR 
WORKDIR in a Dockerfile sets the working directory for subsequent instructions like RUN, CMD, ENTRYPOINT, COPY, and ADD, making them execute relative to that path instead of the root (/).​
