CMD
This instruction run is a lifeline of container.
when we initialize the container theough docker run, its kick in and keep the container alive as infinitly.

Run is kicked off at the time of image building.
