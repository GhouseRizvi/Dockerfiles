USER
USER Instroduction: The Dockerfile is a script that contains a series of instructions on how to build a Docker image. It defines the base image, sets up the environment, installs dependencies, and specifies the commands to run when the container starts.   
user instruction is used to run the commands with designated user.